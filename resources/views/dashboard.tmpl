{{define "dashboard.tmpl"}}
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Dashboard — GoravelTest</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="min-h-screen bg-gray-50 flex">

  <!-- Sidebar -->
  <aside class="w-56 bg-white border-r border-gray-200 flex flex-col">
    <div class="px-6 py-5 border-b border-gray-100">
      <span class="text-lg font-bold text-gray-900">GoravelTest</span>
    </div>
    <nav class="flex-1 p-3 space-y-1">
      <a href="/dashboard"
        class="flex items-center gap-3 px-3 py-2 rounded-lg text-sm font-medium {{if eq .activePage "dashboard"}}bg-indigo-50 text-indigo-700{{else}}text-gray-600 hover:bg-gray-100{{end}}">
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"/></svg>
        Dashboard
      </a>
      <a href="/clients"
        class="flex items-center gap-3 px-3 py-2 rounded-lg text-sm font-medium {{if eq .activePage "clients"}}bg-indigo-50 text-indigo-700{{else}}text-gray-600 hover:bg-gray-100{{end}}">
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0z"/></svg>
        Clients
      </a>
      <a href="/pages"
        class="flex items-center gap-3 px-3 py-2 rounded-lg text-sm font-medium {{if eq .activePage "pages"}}bg-indigo-50 text-indigo-700{{else}}text-gray-600 hover:bg-gray-100{{end}}">
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"/></svg>
        Pages
      </a>
    </nav>
    <div class="p-4 border-t border-gray-100">
      <div class="text-xs text-gray-500 mb-2 truncate">{{.user.Name}}</div>
      <form method="POST" action="/logout">
        <button type="submit" class="text-xs text-red-500 hover:text-red-700">Sign out</button>
      </form>
    </div>
  </aside>

  <!-- Main -->
  <main class="flex-1 p-8">
    <div class="mb-8">
      <h1 class="text-2xl font-bold text-gray-900">Dashboard</h1>
      <p class="text-gray-500 text-sm mt-1">Welcome back, {{.user.Name}}</p>
    </div>

    <!-- Stats -->
    <div class="grid grid-cols-1 sm:grid-cols-3 gap-5">
      <div class="bg-white rounded-xl border border-gray-200 p-6">
        <p class="text-sm text-gray-500">Total Clients</p>
        <p class="text-3xl font-bold text-gray-900 mt-1">{{.clientCount}}</p>
      </div>
      <div class="bg-white rounded-xl border border-gray-200 p-6">
        <p class="text-sm text-gray-500">Active Leads</p>
        <p class="text-3xl font-bold text-indigo-600 mt-1">{{.leadCount}}</p>
      </div>
      <div class="bg-white rounded-xl border border-gray-200 p-6">
        <p class="text-sm text-gray-500">CMS Pages</p>
        <p class="text-3xl font-bold text-gray-900 mt-1">{{.pageCount}}</p>
      </div>
    </div>

    <!-- Quick links -->
    <div class="mt-10 grid grid-cols-1 sm:grid-cols-2 gap-4">
      <a href="/clients/create"
        class="flex items-center gap-3 p-5 bg-white rounded-xl border border-gray-200 hover:border-indigo-300 hover:shadow-sm transition-all">
        <span class="text-2xl">➕</span>
        <div>
          <div class="font-medium text-gray-900 text-sm">Add Client</div>
          <div class="text-xs text-gray-500">Add a new CRM client or lead</div>
        </div>
      </a>
      <a href="/pages/create"
        class="flex items-center gap-3 p-5 bg-white rounded-xl border border-gray-200 hover:border-indigo-300 hover:shadow-sm transition-all">
        <span class="text-2xl">📝</span>
        <div>
          <div class="font-medium text-gray-900 text-sm">New Page</div>
          <div class="text-xs text-gray-500">Create a new CMS page</div>
        </div>
      </a>
    </div>
  </main>

</body>
</html>
{{end}}
